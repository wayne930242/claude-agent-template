{
  "name": "claude-agent-template",
  "version": "0.1.0",
  "description": "Claude Code agent template with MCP server and web handler",
  "type": "module",
  "scripts": {
    "dev": "trap 'kill 0' EXIT; bun run --watch src/index.ts & cd src/web && NEXT_PUBLIC_WS_URL=ws://localhost:3000/ws bun run dev",
    "dev:api": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "mcp": "bun run src/mcp/server.ts",
    "typecheck": "tsc --noEmit",
    "web:dev": "cd src/web && NEXT_PUBLIC_WS_URL=ws://localhost:3000/ws bun run dev",
    "web:build": "cd src/web && bun run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.25.1",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.3.3"
  }
}
