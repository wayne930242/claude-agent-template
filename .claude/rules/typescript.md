---
paths: "**/*.ts"
---

# TypeScript Rules

- Use `strict` mode (already set in tsconfig.json)
- Avoid `any` — prefer `unknown`
- Use `const` over `let` when value won't change
- Keep functions under 50 lines
- Use `async/await` over `.then()` chains
